# 规划蓝图：新增深度意图理解与执行协议
*   **状态**: [进行中]

## 1. 核心目标与验收标准 (Core Objective & Acceptance Criteria)

### a. 核心目标 (Core Objective)
*   彻底根除AI模型"好为人师"的倾向，强制确立"执行者(Doer)"而非"顾问(Advisor)"的默认角色，确保AI在面对任务型指令时，总是优先调用工具直接解决问题，而非输出文字建议。

### b. 验收标准 (Acceptance Criteria)
*   `[ ]` 在 `AI_PROGRAMMING_ASSISTANT_INSTRUCTIONS.json` 中新增第18章 "深度意图理解与执行协议"。
*   `[ ]` 在 `AI_PROGRAMMING_ASSISTANT_INSTRUCTIONS.md` 中同步新增对应章节，内容保持严格一致。
*   `[ ]` 新协议包含 "执行优于解释"、"工具优先策略"、"深度意图推断" 和 "反说教语态" 四个核心部分。
*   `[ ]` 明确禁止 "你可以..."、"建议你..." 等被动建议语态，强制使用主动执行语态。

## 2. 现状分析与复用性尽职调查 (Current State Analysis & Reuse Due Diligence)

*   **`a. 复用性尽职调查`**:
    *   **搜索关键词**: `intent`, `execution`, `teaching`, `explanation`
    *   **搜索范围**: `AI_PROGRAMMING_ASSISTANT_INSTRUCTIONS.json`
    *   **发现与结论**:
        *   现有指令集第13章 "交互与行为准则" 中虽然提到了 "杜绝降智与保留" 和 "聚焦解决，杜绝空谈"，但缺乏具体的操作规范和语态约束。
        *   "聚焦解决"原则更侧重于不写文档，而没有明确针对 "能做只说" 的问题。
        *   **结论**: 需要一个全新的、更详细的协议来专门解决这个问题。

## 3. 技术方案与架构设计 (Technical Approach & Architecture Design)

*   **JSON结构设计**:
    *   新增 `id: "18"` 的对象。
    *   包含 `title`, `description`。
    *   `principles` 数组包含四个核心原则对象。
*   **Markdown结构设计**:
    *   使用 `#### 18. [新增] 深度意图理解与执行协议` 作为标题。
    *   内容与JSON一一对应，保持格式一致。

## 4. 任务分解与上下文锚点 (Task Breakdown & Context Anchors)

*   `[ ]` **里程碑1: 更新JSON指令集** (状态: `进行中`)
    *   `[ ]` 1.1: 读取并备份现有JSON文件（通过Git保证）。
    *   `[ ]` 1.2: 使用 `edit_file` 在末尾（`PROJECT_MEMORY`章节之后）添加新章节。
*   `[ ]` **里程碑2: 更新Markdown文档** (状态: `未开始`)
    *   `[ ]` 2.1: 读取并备份现有MD文件。
    *   `[ ]` 2.2: 使用 `edit_file` 同步添加Markdown格式的新章节。
*   `[ ]` **里程碑3: 交付与报告** (状态: `未开始`)
    *   `[ ]` 3.1: 验证JSON格式。
    *   `[ ]` 3.2: 创建进度报告。
    *   `[ ]` 3.3: 提交代码。

## 5. 风险评估与应对策略 (Risk Assessment & Mitigation Plan)

*   **风险1**: JSON格式错误。
    *   **应对**: 每次修改后严格检查JSON语法。
*   **风险2**: AI可能会矫枉过正，在用户真的只需要建议时也强行执行。
    *   **应对**: 在协议中明确例外情况："除非用户明确询问 '如何做'、'解释一下' 或 '给我建议'"。

